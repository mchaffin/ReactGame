import React from "react"
import "./Jumbotron.css"

const Jumbotron = () => (
  <div className="jumbotron text-center">
    <h1>Clicky Chiclets!</h1>
    <p>
      Click on an character to earn points, but don't click the same image twice!
    </p>
  </div>
);

export default Jumbotron;